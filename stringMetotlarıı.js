var url = "https://www.mangarode.com";
var kurs = "JS Web Development";

console.log(url.length);

console.log(kurs.split(" ").length);

var ssl;

ssl = (kurs.split(" ")[0]="https") ?"Güvenli Değil":"Güvenli Kullanım"
console.log(ssl)


var string;

string = `merhaba bu benim yeni hessabim link: ${url}`;
console.log(string);

var bul;
